import RoomsSideBar from "../components/RoomsPage/RoomsSideBar";

export default function YourRoomsPage() {
	return (
		<div className="min-h-screen flex items-center justify-center" >
			<RoomsSideBar />
		</div>
	);
}
